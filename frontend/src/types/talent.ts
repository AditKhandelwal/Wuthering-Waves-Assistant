export interface Talent {
  skillType: string;
  name: string;
  description: string;
  pictureUrl: string;
  recommendLevel: number;
}

export interface InherentSkill {
  name: string;
  description: string;
  pictureUrl: string;
}
